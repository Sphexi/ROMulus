"""Package-data namespace for built-in profile YAML files.

Files live under ``romulus/data/profiles/*.yaml`` and ship inside the wheel
via the ``tool.setuptools.package-data`` config in ``pyproject.toml``. Loaded
through :mod:`importlib.resources` from :mod:`romulus.core.exporter`.
"""
